import 'package:flutter/material.dart';

class DetailPagePressing extends StatelessWidget {
  const DetailPagePressing({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}