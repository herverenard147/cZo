import 'package:flutter/material.dart';

class DetailPagePlacement extends StatelessWidget {
  const DetailPagePlacement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}